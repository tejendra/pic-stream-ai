# Yarn configuration for pic-stream-ai project

# Use exact versions for better reproducibility
save-exact true

# Enable workspaces
workspaces-experimental true

# Use the classic node_modules structure for better compatibility
node-linker node-modules

# Enable PnP for faster installations (optional, can be enabled later)
# node-linker pnp 